---
title: Installation
description: Requirements, installing from a release or from source.
---

Requires WordPress 6.5+, PHP 7.4+, and at least one supported store plugin active.

There is deliberately no `Requires Plugins` header: it made WordPress refuse activation without one
particular store plugin, which meant no other platform could ever be reached.

## Full reference

This page is a summary. The complete, canonical version lives in the repository and is what the
plugin's own contributors read:

[`docs/guides/installation.md`](https://github.com/mralaminahamed/storeseeder/blob/trunk/docs/guides/installation.md)

:::note[Still to port]
The repository guide is the source of truth today. Content is being moved here rather than copied —
two versions of one document is how they come to disagree.
:::
